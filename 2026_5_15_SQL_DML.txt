-- select
-- 조회하다, 데이터를 읽다
-- 지금까지 했던 DDL 은 형태가 좀 꽉 잡혀딨다면,
-- DML중에 SELECT는 형태가 많이 유연해요

SELECT 
id,
username,
name
FROM t_user; 

SELECT -- 3
id,
title,
user_id,
view_count
FROM t_board  -- 1
WHERE view_count > 1   -- 2
;

-- 시험엔 이렇게까진 난이도 안나올거에요
UPDATE t_board AS b
SET view_count=b.id,
like_cnt=b.id;



SELECT 
id
FROM t_board
WHERE id =1 OR id=2
;
SELECT 
id
FROM t_board
WHERE id IN (1,2)
;


SELECT 
id,
user_id
FROM t_board
WHERE id =1 AND user_id=1
;

SELECT
*
FROM t_comment AS c
GROUP BY c.board_id
;